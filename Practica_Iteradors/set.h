#pragma once
#include <set>
#include <iostream>
using namespace std;

class setIT
{
public:
	setIT();
	~setIT();

	void printSetForward(std::set<int> myDeque);
	void printSetBackward(std::set<int> myDeque);
};

